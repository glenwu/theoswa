# 潮汕升级 · 给 Claude 的项目须知

四人两副牌的网页对战。服务端（Node + ws）持有唯一权威状态，客户端（React + Vite + Tailwind）
只发送意图。规则纯函数放在 `server/`，客户端通过相对路径直接 import 同一份实现
（例：`client/src/playCheck.js` → `server/trick.js`），**规则绝不实现两遍**。

---

## 🔒 保密约束（最高优先级）

**权威规则文档 `潮汕升级-开发提示词-v2.md` 在仓库之外，绝不能进公开 git。**
`.gitignore` 里有 `*提示词*.md` / `*开发提示词*` 两条防呆，即使有人把它拷进仓库目录 git 也不会收。
**不要移除这两条，也不要用 `git add -f` 绕过。**

规则要点可以写进本文件（Glen 已确认），文档本身不行。

定稿之后由 Glen 追加、因而**覆盖文档原文**的裁定，记在仓库外的
`../潮汕升级-规则变更记录.md`（与本仓库平级）。改规则前先读它。

---

## 常用命令

```bash
npm test                 # node:test，全部单测（当前 407 条）
npm run server           # 只绑 127.0.0.1
npm run server:lan       # HOST=0.0.0.0，对外开放时用
npm run start:lan        # 先构建再对外开放
npm run build            # 构建客户端（服务端静态托管 client/dist）
npm run simulate:bots    # 无界面跑整局，用于统计 BOT 行为
node scripts/audit/mutantsN.mjs   # 变异测试，见下
```

`server:debug` / `server:fast` 只供本机验证（打开 `/api/debug/inject`、把出牌超时压到 2.5 秒），
**对外开放时绝不要用**。

---

## 规则裁定（覆盖文档原文，最容易搞错的几条）

**撬底不额外加 20 分。** `P_final = 闲家台面分 + 底牌分`。
- `P_final < 80` → 只移庄，双方都不升级
- `P_final ≥ 80` → 移庄，闲家升 `floor((P_final − 80) / 20) + 1` 级
- 逐档：80→1、100→2、120→3、140→4、160→5、180→6、200→7
- **上限自然收口**：全场总分 200，闲家通吃恰好 7 级。**若出现 8 级，说明 +20 又混进来了。**

**未撬底那一列不变**：80–99 移庄升 0 级、100–119 升 1 级、每多 20 再升 1 级；
`P < 80` 时庄家连庄并升级（P=0 升 5 级，1–79 升 `ceil((80−P)/20)` 级）。

**主牌甩牌：底牌里的主牌不挡。** 文档 §6.8.2b 自身矛盾，以反面表述为准 ——
底牌里的牌这一局不可能再上台面，压不到你。见 `server/pieces.js` 的 `trumpDumpVerdict`，
那里有长注释，**不要照文档正面表述「修正」成挡**。

**逆时针唯一真源是 `server/rotation.js`**：`0 → 3 → 2 → 1`，`nextSeat = (seat+3) % 4`。
项目内任何轮转必须引用它，**禁止别处硬编码 ±1**。

**副级牌属于主牌，不属于原花色。** 打 2 主红桃时 `♠2` 是主牌，不是黑桃。
`playSuitOf` 是唯一判据。

**件 = 副牌花色里仍是副牌的 A 和 K。** 主花色的 A/K 不是件；级牌占用该点数时也不是
（打 A 时 A 升主，该门只剩 K×2）。

**副牌甩牌资格** = 该门每一件都 `status !== 'unseen'`（在我手上／已打出／底牌亮出）。
⚠️ **庄家埋进底牌的副 A/K 会被系统强制公开亮出**，随即变成 `'seen'` ——
等于替对手把甩牌条件凑齐了。这条决定了 BOT 埋底时绝不压副 A。

**庄家不吃分，是跑分。** 只有闲家一个分数账，庄家赢的分直接作废。
全局守恒：`闲家台面 + 庄家跑掉 + 底牌 = 200`。**底牌分是加成，不参与守恒校验。**

**级别绕回**：打 A 之后升一级回到 2（第二圈），在第二圈的 2 上再升一级才获胜（共 14 级）。

**彩蛋随机源必须是 `state.niiRandom`，绝不能用发牌 rng** ——
掷骰会推进 RNG 状态，`SEED=42` 就复现不出同一副牌（验收用例 §10-52）。

---

## 架构硬约束

**保密裁剪**：`server/viewer.js` 的 `viewerState` 是唯一出口，末尾用
`collectLeakedCards` 递归扫描整个 payload，出现白名单外的 Card 形状对象直接抛错。

⚠️ **这个扫描器有两个结构性盲区，加字段时必须自己把关**：
1. **字符串**：`cardLabel()` 生成的牌名（日志、聊天、bot 复盘）它看不见。
   目前只有 5 处往日志写牌名，全部是公开牌。
2. **派生数字**：张数、花色构成、件位置都是数字，不是 Card 形状。
   把 `ownComposition` 加到所有 player 上等于摊牌，而扫描器和现有测试都抓不到。

**阶段节奏唯一真源是 `server/constants.js` 的 `DEFAULT_TIMINGS` + `TIMING_ENV_KEYS`。**
**绝不要在 `index.js` / `state.js` 或别处再抄一份。** 已经踩过两次：抄的那份没跟着改，
单测断言常量本身没问题，跑起来的服务端却用着旧值 —— 极难发现。

**每个「等一个人点」的阶段都必须有服务端兜底计时器**，否则那人掉线全场永久卡死。
`server/test/timers.test.js` 把这条变成硬约束（缺一个就红），并专门检查延时不是 `NaN`
（`setTimeout(NaN)` 会立刻触发）。设计上只有 SEATING / READY_CHECK / GAME_OVER 允许无计时器。

**暂停**：所有截止时刻都是绝对时间戳。恢复时必须由 `server/pause.js` 的 `shiftDeadlines`
整体后移「暂停了多久」，否则恢复瞬间全部触发。字段清单集中在 `ROUND_DEADLINE_FIELDS`，
有测试逐字段核对 `createRoundState` 的产物 —— **新增任何 `*Deadline` 忘了登记就会红**。
客户端同样要用 `displayNow()` 把倒计时冻在 `paused.at`，不然界面照跌。

**电脑玩家只吃 `viewerState` 返回的本人视角**，绝不读服务端完整 state。
「托管」（`player.autoPlay`）与「电脑」（`player.isBot`）是两回事：前者人还在线、
身份不变、随时可取消；后者座位已经没人了。托管**不**触发自动暂停。

**部署**：`HOST` 默认 `127.0.0.1`，对外要显式设 `0.0.0.0`。
`ADMIN_RESET_TOKEN` **没有默认值**，不设即关闭管理员能力。
`/api/health` **绝不能返回 `state.seed`** —— 种子完全决定牌堆顺序，
拿到它就能在本地把四家手牌和底牌全算出来（`scripts/audit/seed-leak.mjs` 有实证）。

⚠️ **本服务没有任何身份验证**：身份就是 `T/H/B/M` 四个字母，写死在共享常量里。
知道地址的人可以选任意一家并看到那家的手牌。Glen 已知情并选择先不加，走 Tailscale 而非公网。

---

## 工作方式（Glen 的要求）

- **一次一个改动，带测试。** 改完跑 `npm test`。
- **改规则前先重述自己的理解**，等确认再动手。不确定就问，别猜。
- **改了别人写的测试断言要明说**，包括为什么原来那条是错的。
- 报告要如实：测试红了就贴输出，跳过了就说跳过，没验证到的边界要讲清楚。

### 变异测试（`scripts/audit/mutants*.mjs`）

改完 BOT 策略或规则后**跑一遍**，几十秒。它们比测试本身抓到的问题还多 ——
这一轮逼出了十几条「碰巧通过」的测试。

```bash
node scripts/audit/mutants4.mjs   # 吊主 / 保底 / 埋底 / 求件
node scripts/audit/mutants8.mjs   # 暂停 / 截止时刻后移
```

⚠️ **它们会就地改写源码再跑测试**，所以：
- **只在工作区干净时跑**（万一异常退出，`git checkout` 就能还原）
- **别和 dev server 同时跑**：踩过一次 —— 变异测试在后台跑着，这边重启了服务端，
  Node 把当时正处于变异状态的 `viewer.js` 加载进去缓存住了，浏览器里看到的是变异版的
  行为，差点当成真 bug 去查。要么等它跑完，要么跑完再重启服务端。
- **改了函数签名要立刻重扫锚点**（`MUTATE_DRY=1`）—— 把 `aggressive` 改成 `mode`
  那次没扫，两条锚点悄悄失效了一整轮
- **看到 SKIP 立刻更新锚点** —— 一套满是 SKIP 的变异测试是虚假的安全感
- `mutate.mjs` 已处理 `finally` 与 SIGINT/SIGTERM；SIGKILL 救不了（踩过一次：
  批量跑超时被杀，源码停在变异状态，下一轮 `npm test` 莫名红了三条）

---

## 踩过的坑（都真实发生过）

**`round.lastTrick` 和 `trickHistory` 的最后一项是同一个对象**
（`actions.js` 里先 push 再赋值）。统计已打出的牌时算两遍会让刚打完那一墩翻倍。

**加分会叠加到同一张牌上。** `addProposal` 对同一组牌累加 bonus，
所以「A 提案 450 + B 提案 160 = 610」可能压过一个 520 的提案。
写测试断言某个分支时，必须先确认没有别的提案叠加到同一张牌上把它盖掉。

**测试 fixture 的常见错法**（每一条都让测试「碰巧通过」过）：
- 座位是 rng 分配的，**别假设 `T` 就在座位 0**
- 跟牌规则要求「手里有该花色就必须全跟」，fixture 里给它留一张同花色小牌，
  就永远打不出主牌，测试会以 `MUST_FOLLOW_SUIT` 失败
- `{ suit: 'H', rank: 16 }` **不是大鬼**，真大鬼是 `{ suit: 'JOKER', rank: 16 }`。
  `cardStrength` 只看 rank，所以这种假牌在牌力上像大鬼、在 suit 过滤时又不像
- 让 bot 坐在「打出去稳赢」的位置上，它本来就会打，被测的加分项根本没参与决策

**恒真的守卫是死代码，而且会骗过变异测试。** 例：`!isLead` 与 `beatenSeat !== null`
完全等价（首家出牌时 `currentTrick` 为空），多写一条删掉行为不变，
变异测试却会显示「已覆盖」。

**断言要钉住条件，不只是结果。** 好几次条件写反了、打分改了，
测试仍然绿 —— 因为两条路径恰好给出同一个答案。用变异测试验证断言真的能区分新旧行为。

**别虚构「决策翻转」。** 有些改动只是让评分差了 300 分，决策未必变。
这时应当直接比较 `evaluateFollowChoices` 的评分，而不是断言它换了一张牌。

---

## BOT 策略（Glen 是牌友，这些是他口述的打法）

策略层的原则是**强先验而非硬规则** —— 常规牌理只给特定候选加分，特殊局面仍能被总分推翻。

- **保底判定是动态的张数对比**（`assessBottomControl`）：从顶档往下累计，
  「别人能压我的张数」少于「我这一档及以上的张数」就成立 —— 他的大牌一张只能换我一张，
  换不完，我必然还剩一张拿得下最后一墩。同强度算别人能压我（先出者大）。另需主牌 ≥9 张。
  例：我有大鬼 + 小鬼、外面只剩一张大鬼没现身 → 够了（Glen：「出了小鬼他就有了」）。
- **吊主看角色**：有保底牌→吃大不吊；我做庄→保守续吊；队友做庄→跟他路子；闲家→无所谓。
  副牌够强一律转副牌。
- **「带分吊主」是双向约定**：庄家首墩领【带分的主】= 求大鬼（我有大鬼但保不了底）。
  队友用鬼吃下、或之后转领副牌，都是「不用吊主」的应答。
  **庄家收到应答就该收手**去跑副牌 —— 只写应答的一半会让它一路吊到手上只剩两个鬼。
- **吊主默认吊小牌**（逼对手用大牌来杀）。甩尾手计划挂起时吊**副级牌**那一档 ——
  Glen：「通常都是打副7，主7以上一般拿来杀的」。常态下鬼和主级牌不进吊主的候选
  （`drawableTrumps`），那是毙牌和保底的本钱。
- **但这不是硬规则**（Glen 纠正过一次）：「潮汕升级的玩法就是随时都需要看当时形势
  来定要出的牌」。**清顶**（`trumpClearingOut`）成立时鬼重新进候选、领最大的、
  连吊先大后小：① 顶端还剩 1–2 张没现身（`control.topOutstanding`，有东西可撞）
  ② **外面剩的小主 ≤1 张** —— 他没有小主可垫，只能拿大牌来跟，这才叫撞得出来。
- ⚠️ **「很可能把大牌撞出来」≠「顶端只剩一两张」。** 领大鬼【逼不出】对手的大鬼：
  他只要还有一张小主，跟一张小的就躲过去了。第一版就是这么译错的，
  400 局配对实测保底 283 → 269（翻转 2 好 16 坏），改对之后 284（1 好 0 坏）。
  **策略改动一律用 `scripts/audit/clearing-paired.mjs` 那种「同种子逐局配对」来验，
  总量对比会被「两边用同一套策略」搅浑，看不出退步。**
- **写策略时先想「这条会不会在某个形势下反过来」，再决定用过滤还是用加分。**
- **求件缺什么打什么**：AAK 差 K 就打 K，AKK 差 A 就打 A。
  **自己这门没有件就别探** —— 那是替对手求件。
- **甩尾手**：能甩不等于现在就该甩。计划需要「能甩的长副牌 + 起手牌 + 对手主牌不够毙」。
  判据是**单独一家最多可能有几张主**，不是场上主牌总数。
- **盖过已经领先的队友要看桌上有没有分**：有分可能是护分（轻罚），一分没有就是纯浪费（重罚）。
- **跟多张只比【最大的那一张】**（`server/trick.js` 的 `trickLeader` → `maxStrength`）。
  所以毙一手两张 = 一张够大的 + 一张凑数的，另一只鬼留着保底。
  `followCandidates` 的 `economical()` 专门生成这一组 —— 原来只有「全大/全小/全分」
  三种形状，「全小」赢不下来时「全大」就成了唯一能赢的选项（Glen 实战里两只大鬼一起交了出去）。
  **加评分项之前先看候选生成：选不出来的东西，评分再准也没用。**

### 试过但没用的：毙牌时权衡保底/撬底

Glen 的牌理没错（「底至少值 20 分，不能见牌就砍」），但**七种实现全部测负**，
不要再拿同样的形式重来一遍。400 局逐局配对（`scripts/audit/kill-paired.mjs`）：

| 形式 | 保住底 | 浪费型毙牌 |
|---|---|---|
| 基线 | 284/400 | 64 |
| 只改候选生成（已落地） | **284** | **33** |
| +「holdsTopTrump 由真变假」跳变罚 | 281 | 33 |
| +「不到 20 分按差额连续罚」 | 278 | 25（但总毙牌 640→714）|
| +「只罚一次交 ≥2 张顶牌」 | 282 | 29 |

猜测：**不毙就等于把这一墩的分送给对手**，而闲家分一涨就更容易上 80 撬底；
省下来的主牌并不能可靠地换成「守住最后一墩」。要再做得先找到更准的
「这一毙确实危及底」的判据，光按分数/主牌数拦是不行的。

`server/bot-evolved.json` 是自我对弈进化出来的权重，`npm run train:bots` 可重训。
难度 `BOT_DIFFICULTY=easy|normal|hard|expert`（默认 expert），
靠推断强度 / 控制牌保留 / 历史学习分档，**不靠偷看暗牌**。
